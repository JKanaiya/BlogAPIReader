import Auth from "../components/Auth";
import { describe, test, expect } from "vitest";
import { screen, render } from "@testing-library/react";

// describe("Test Sign Up", () => {
//   test("If Password and Confirm Password do not match while the user is typing in the Confirm Passsword field, show 'Passwords do not match'", () => {
//     render(<Auth/ >);
//
//   })
//
// })
